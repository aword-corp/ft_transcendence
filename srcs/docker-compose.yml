services:
  nginx:
    build: nginx
    ports:
      - "8080:8080"
      - "8080:8080/udp"
    networks:
      - reseau
    restart: always
    env_file:
      - .env

  # TODO : faire le container pour postgresql
  postgresql:
    build: postgresql
    networks:
      - reseau
    restart: always
    env_file:
      - .env
    # healthcheck:
    #   test: ["CMD", "/bin/true"]
    #   interval: 1m30s
    #   timeout: 30s
    #   retries: 5
    #   start_period: 30s
    # TODO : configurer le healthcheck pour postgresql

  django:
    build: django
    networks:
      - reseau
    restart: always
    env_file:
      - .env
    # depends_on:
    #   postgresql:
    #     condition: service_healthy

networks:
  reseau:
    driver: bridge
